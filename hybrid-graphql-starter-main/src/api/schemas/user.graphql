type User {
  user_id: ID!
  username: String!
  email: String!
  level_name: String!
  created_at: String!
}

type Query {
  users: [User]
  user(user_id: ID!): User
}

input UserInput {
  username: String! @constraint(minLength: 3, maxLength: 255)
  email: String! @constraint(format: "email")
  password: String! @constraint(minLength: 5, pattern: "(?=.*[A-Z])")
}

type UserResponse {
  user: User!
  message: String!
}

type LoginResponse {
  user: String!
  token: String!
  message: String!
}

type Mutation {
  createUser(input: UserInput!): UserResponse
  login(username: String!, password: String!): LoginResponse
}
